{
  "name": "filegrc-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/filegrc",
    "packages/create-filegrc"
  ],
  "scripts": {
    "dev": "node --watch scripts/dev.mjs",
    "test": "npm test --workspaces --if-present",
    "validate": "npm test --workspaces --if-present && node packages/filegrc/bin/filegrc.js model --check-docs"
  },
  "engines": {
    "node": ">=20"
  }
}
